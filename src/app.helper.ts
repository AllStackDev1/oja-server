// import { secret, expiresIn } from 'app.enviroment'
// import { sign } from 'jsonwebtoken'

// export const signJWT = (payload, exp = expiresIn): string => {
//   return sign(payload, secret, { expiresIn: exp }).toString()
// }
